// NOTE:
// This file was originally written for a Create React App-style
// setup using `http-proxy-middleware` and CommonJS (`require/module.exports`).
// The current project uses Vite (with native ESM) and Vite's built-in
// dev server proxy configuration in `vite.config.js`, so this file
// is no longer used.
//
// It is intentionally left as a no-op to avoid confusion and runtime issues.

export {};
